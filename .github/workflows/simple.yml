name: Shell Commands 

on: 
  repository_dispatch:
    types: [test-workflow]
    
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Hello World"
      - name: echo key from payload
        run: echo ${{ github.event.client_payload.example-key }}
